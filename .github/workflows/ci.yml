name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore

      - name: Check formatting
        run: dotnet format --verify-no-changes --verbosity diagnostic

      - name: Build
        run: dotnet build --no-restore --configuration Release

      - name: Test
        env:
          DOTNET_SYSTEM_CONSOLE_ALLOW_ANSI_COLOR_REDIRECTION: true
        run: dotnet test --no-build --configuration Release --logger "console;verbosity=detailed" --logger GitHubActions

  # security:
  #   Disabled — SQLitePCLRaw 2.1.11 (transitive from EF Core Sqlite) has
  #   GHSA-2m69-gcr7-jv3q with no fix in 2.x. Prod uses PostgreSQL.
  #   Re-enable when EF Core Sqlite ships with SQLitePCLRaw 3.x.
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout
  #       uses: actions/checkout@v7
  #     - name: Setup .NET
  #       uses: actions/setup-dotnet@v5
  #       with:
  #         dotnet-version: '10.0.x'
  #     - name: Restore
  #       run: dotnet restore
  #     - name: Check vulnerable packages
  #       run: dotnet list package --vulnerable --include-transitive 2>&1 | tee audit.txt && ! grep -q 'has the following vulnerable packages' audit.txt
